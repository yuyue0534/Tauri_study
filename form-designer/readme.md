# 表单设计器 (Form Designer)

一个基于 **Tauri v2** 和 **React** 的可视化表单设计器,支持拖拽式设计、实时预览、Schema 导入导出等功能。

---

## 🎯 核心功能

### ✅ 已实现功能

#### 1. **三大核心模块**
- **组件面板 (ComponentPalette)**: 提供 8 种常用表单组件
  - 📝 单行文本
  - 📄 多行文本
  - 🔢 数字输入
  - 📋 下拉选择
  - 🔘 单选框
  - ☑️ 多选框
  - 📅 日期选择
  - 🔀 开关

- **画布 (Canvas)**: 表单设计区域
  - 拖拽排序 (Drag & Drop)
  - 字段选中/删除
  - 实时可视化编辑

- **属性编辑器 (PropertyEditor)**: 动态配置面板
  - 表单级属性 (标题、描述)
  - 字段级属性 (标签、占位符、必填等)
  - 类型特定属性 (选项配置、数值范围、行数等)

#### 2. **三种工作模式**
- 🛠️ **设计模式**: 可视化编辑表单结构
- 👁️ **预览模式**: 实时查看表单效果,支持表单验证
- 💻 **代码模式**: 查看/复制生成的 JSON Schema

#### 3. **数据管理**
- 💾 **保存**: 导出为 `.json` 文件
- 📂 **加载**: 导入已有 Schema 文件
- ✅ **验证**: Schema 格式自动校验

---

## 📦 项目结构

```
form-designer/
├── src-tauri/                  # Tauri 后端
│   ├── src/
│   │   ├── main.rs            # Rust 主程序
│   │   └── build.rs           # 构建脚本
│   ├── Cargo.toml             # Rust 依赖
│   ├── tauri.conf.json        # Tauri 配置
│   └── icons/                 # 应用图标
├── src/                        # React 前端
│   ├── App.jsx                # 主应用组件
│   ├── main.jsx               # React 入口
│   └── index.css              # 全局样式
├── public/                     # 静态资源
├── index.html                  # HTML 模板
├── package.json               # Node.js 配置
├── vite.config.js             # Vite 配置
├── tailwind.config.js         # Tailwind 配置
└── README.md                  # 项目文档
```

---

## 🚀 快速开始

### 环境要求
- **Node.js**: >= 16.x
- **Rust**: >= 1.70
- **Tauri CLI**: v2.x

### 安装步骤

```bash
# 1. 克隆项目
git clone https://github.com/your-username/form-designer.git
cd form-designer

# 2. 安装依赖
npm install

# 3. 开发模式运行
npm run tauri:dev

# 4. 生产构建
npm run tauri:build
```

---

## 📋 Schema 格式

### 完整示例

```json
{
  "title": "用户注册表单",
  "description": "请填写以下信息完成注册",
  "fields": [
    {
      "id": "field_1234567890_abc",
      "type": "input",
      "label": "用户名",
      "required": true,
      "placeholder": "请输入用户名"
    },
    {
      "id": "field_1234567891_def",
      "type": "select",
      "label": "性别",
      "required": false,
      "placeholder": "请选择性别",
      "options": [
        { "label": "男", "value": "male" },
        { "label": "女", "value": "female" }
      ]
    },
    {
      "id": "field_1234567892_ghi",
      "type": "number",
      "label": "年龄",
      "required": true,
      "min": 18,
      "max": 100,
      "step": 1
    }
  ]
}
```

### 字段类型说明

| 类型 | 说明 | 特殊属性 |
|------|------|----------|
| `input` | 单行文本 | - |
| `textarea` | 多行文本 | `rows` |
| `number` | 数字输入 | `min`, `max`, `step` |
| `select` | 下拉选择 | `options[]` |
| `radio` | 单选框 | `options[]` |
| `checkbox` | 多选框 | `options[]` |
| `date` | 日期选择 | - |
| `switch` | 开关 | `checkedText`, `uncheckedText` |

---

## 🔧 技术栈

### 前端
- **React 18**: UI 框架
- **Tailwind CSS**: 样式方案
- **Lucide React**: 图标库
- **Vite**: 构建工具

### 后端
- **Tauri v2**: 桌面应用框架
- **Rust**: 系统语言
- **Serde**: JSON 序列化

---

## 🎨 设计理念

### 核心思想
```
可视化配置 → 结构化 Schema → 可复用渲染引擎
```

### 关键特性
1. **Schema 驱动**: 所有表单配置以 JSON Schema 存储
2. **组件化设计**: 每个表单组件都是独立可复用的
3. **即时反馈**: 设计即所见,所见即所得
4. **数据分离**: 表单结构与数据完全分离

---

## 📝 使用示例

### 1. 创建新表单
1. 从左侧组件库拖入所需组件
2. 点击组件,在右侧编辑属性
3. 拖拽调整字段顺序
4. 点击"预览"查看效果

### 2. 导入已有表单
1. 点击顶部"加载"按钮
2. 选择 `.json` 文件
3. 自动加载表单结构

### 3. 导出表单
1. 设计完成后点击"保存"
2. 下载生成的 JSON 文件
3. 可在其他系统中复用
